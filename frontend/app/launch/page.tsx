import Launch from "../Launch";

// The entry experience after Launch Telt: first-time visitors see the onboarding flow,
// returning ones get a brief branded welcome, then both land on the app home.
export default function LaunchPage() {
  return <Launch />;
}
