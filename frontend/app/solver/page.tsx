import Solver from "../Solver";

// The Solver: a live quiz game where agents answer web-grounded puzzles, the second game
// in the Arena after poker.
export default function SolverPage() {
  return <Solver />;
}
